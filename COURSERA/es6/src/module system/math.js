export const addition = (x, y) => x + y;

export const subtract = (x, y) => x - y;

export const multiply = (x, y) => x * y;

export const division = (x, y) => x / y;

export const modules = (x, y) => x % y;

